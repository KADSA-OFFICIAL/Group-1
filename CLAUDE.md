# Group-1 Claude Harness

이 저장소는 issue-first workflow를 사용합니다. Claude Code는 새 기능, 버그 수정, 개선, 리팩터링, 동작 변경을 시작하기 전에 이 하네스를 따라야 합니다.

## 게임 정보 (방과 후)

2D 탑다운 공포 방탈출, Godot 4.6. 주인공 이설이 국어책을 가지러 밤의 학교에 들어가 실종 사건의 진실과 마주친다.
적대자는 괴물이 아니라 **수위 박종태** — 10년 전 딸 박시우가 학교폭력 후 투신했고 학교가 무대응하자 스스로 '심판'을 집행한다. 실종 학생 5명 중 일부는 오해로 지목됐다는 것이 주제.
시나리오 전문: `docs/story.md` (구글 문서 사본 — 대사·지문 원문 + 구현이 기획서와 갈라진 지점 정리).
원본: https://docs.google.com/document/d/1c8rXO7bLU_HvOPKYClmRSG2P97Vloo7maXblYMaQ70E — 원본이 바뀌면 사본도 갱신할 것.
전체 스펙과 4단계 작업 계획은 GitHub 이슈 #137(epic)에 기록돼 있다 — 스토리 작업 전에 먼저 읽을 것.

### 씬 흐름

main_menu → intro(프롤로그 컷신: street→back_gate→art_room→cabinet→next_room, scripts/ui/intro.gd의 SCRIPT_NODES) → main(본편, **4층에서 시작**) → ending → main_menu
5층은 프롤로그 컷신 전용이며 본편에서 방문하지 않는다(MAX_FLOOR=4). 규칙 칠판·사망 엔딩은 구 시나리오 요소로 제거됨.

### 구조 요약

- `scenes/main/main.tscn` = 조립 씬: Darkness(CanvasModulate 어둠), Background(층 씬 인스턴스), Player, GameState, HUD, UI(FloorLabel·FadeRect). 루트 스크립트 `scripts/game/floor_manager.gd`가 층 전환·시작 힌트·페이드 담당.
- 층 씬 `scenes/background/school_floor_1~5.tscn`: **3400×2500, 손도면 기반(#159)**. 씬을 손으로 고치지 말고 **`tools/gen_floors.py`의 LAYOUT 테이블을 고친 뒤 재생성**한다(5개 층 전체를 만든다).
- 층 구조: 북쪽 교실 열(2·3·4층 9칸, 5층 8칸) → 북쪽 복도 → 중간 띠(좌: 계단실·남녀화장실·막힌공간 / 우: 창고·막힌공간·남녀화장실) → 공백 구역을 가로지르는 **중앙다리**(유일한 남북 통로) → 남쪽 복도·특별실동 → 하단 띠(대형실·계단실·화장실). 1층만 도면이 달라 아래쪽 절반만 건물(교실1~3·운동장출입구·교무실 / 현관·수위실·창고).
- **모든 방이 축정렬이다(#265).** 오른쪽 중간 띠와 1층 교무실이 평행사변형이던 것을 일자로 폈다 — 사선 때문에 벽 장식·환풍기·화장실 줄눈·복도 사물함·유도선·배관이 전부 "사선 방은 건너뛴다" 특례를 달고 있었고, 유지 비용이 모양값보다 컸다. `slope_y`·`add_sloped_room`·`SLOPE`는 없다. 방 모양을 다시 기울이려면 그 특례들을 되살려야 한다.
- **방 사이 틈 없음**: 북쪽 교실은 `NORTH_GAP=0`으로 벽을 맞대고, 같은 띠의 다른 방들은 `close_gaps()`(1층은 `_close_floor1()`)가 경계를 틈 한가운데로 옮겨 붙인다. 30~150px 틈은 도면상 의미가 없는데 복도로 열린 막다른 골목이 되고 수위 스폰 후보에 들었다. 계단 접근로처럼 넓은 간격(>200px)은 통로일 수 있어 그대로 둔다. 1층 하단 띠는 깊이를 2480으로 통일했다 — 화장실·창고만 2370이라 그 아래에 걸어 들어갈 수 있는 빈 띠가 남아 있었다.
- 계단실: 좌측 `(300,720)~(740,1000)`, 하단 중앙 `(1450,2120)~(1890,2440)`. **1층은 계단이 한 곳뿐**이고 위치도 다르다 `(220,2120)~(660,2440)`. **2층 하단 계단은 영구 봉인**(그 아래가 1층 현관이라 내려갈 자리가 없음) — `gen_floors.py`의 `SEALED`.
- 건물 밖 공백(중앙다리 양옆, 1층 북쪽 절반)은 **벽으로 막는 것만으로 부족**하다. 안쪽 칸이 통행 가능으로 남아 수위 스폰 후보에 들어가므로 `fill_void()`로 실체를 채운다.
- **방도 복도도 아닌 빈 구역**도 같은 문제였다(#295) — 복도 마루는 가로 직사각형 띠로만 깔리는데 방은 그 띠 안에서 x 구간 일부만 차지해서, 중간·남쪽·하단 띠에서 방이 없는 x 구간과 중앙다리 본체가 바닥칠도 실체도 없이 층마다 수십만 px² 남았다. `add_infill()`이 각 띠의 방 x 구간을 `gaps_in()`으로 훑어, 중앙다리로 이어지는 구간(중간 띠 접근로 → 다리 본체 → 남쪽 띠 접근로)에는 `ground_band_gap()`으로 복도와 같은 마루를 깔고 나머지는 `fill_band_gap()`으로 막는다. **#265(모든 방 축정렬) 뒤로는 직사각형 하나로 끝난다** — 이전에 사선 경계를 사다리꼴로 채우던 버전은 폐기됐다.
- **벽 앞면(#268, #271)**: `sc.wall()`이 **가로 벽에만** 3단을 더 낸다 — 윗변 하이라이트(`WH_`) / 앞면(`WF_`) / 바닥에 닿는 그림자(`WS_`). 앞면 하나만으로는 윗면과 밝기가 비슷한 띠 두 개라 여전히 납작해 보였다. 직각이 문제가 아니라 명암 단계가 없던 것이다. 위에서 내려다본 16px 띠 하나만 그리면 두께만 있고 높이가 없어 건축 도면처럼 보인다 — 면 하나가 벽을 세운다. 세로 벽에는 그리지 않는다(탑다운에서 앞면이 보이는 건 시선과 마주 보는 가로 벽뿐). **시각 전용이라 충돌·광원 차단체는 그대로 16px 띠에 있다.**
  앞면은 `WallGlow`라 그 아래 것을 덮는다. 벽에 붙는 것들은 함께 옮겨야 한다 — 복도 사물함·비품은 `WALL_FACE`만큼 내리고(`base = cy0 + WALL_FACE`), 가로 벽에 걸리는 방 안 장식(창문·커튼·게시판·스피커·환풍기)은 `PD_`(Props)가 아니라 `WD_`(WallGlow)로 내야 앞면 **위에** 그려진다. 세로 벽 장식(칠판·분필받이·태극기·TV·시계)은 앞면이 없어 그대로 둔다.
- 벽 톤: `C_WALL`에 텍스처 게인(`1/TEX_MEAN`)이 붙어 실제로는 1.28배로 칠해진다. 예전 `0.45,0.48,0.55`는 **화면에서 가장 밝은 것이 벽**이 되어 시선을 다 가져갔다(#265). 기둥은 벽보다 **어둡게** 둔다. 벽 재질(`wall_brick`)은 대비 낮은 미장이다 — 두께 16px에 벽돌 두 단은 무늬가 아니라 지퍼로 보인다.
- 벽 규약: 두께 16px, 방 문 폭 110px(가로 중앙, 방 중심 y<900이면 아래변/아니면 위변). 벽은 충돌(WC_*)+시각(WV_*)+광원 차단(Occ_/LO_*) 3종 세트 — 벽 수정 시 셋 다 갱신. 생성 도구는 `tools/`(재실행 경고는 각 스크립트 주석 참조).
- 계단: 가운데 난간으로 반 분할(왼쪽=위층▲, 오른쪽=아래층▼, 방향 표지+목적지 층 번호 표시). 층마다 계단 위치·개수가 다르므로 `floor_manager.gd`의 **층별 `STAIRS` 사각형**에서 트리거 존·도착점을 계산한다(고정 상수 아님). 입구는 층별 열쇠 `stair_key_N`으로 개방(소모형, 그 층 계단 전부 개방).
- 상태 영속: `scripts/game/game_state.gd` — 인벤토리(최대 5개), 플래그(set_flag/has_flag)로 문 개방·아이템 획득 기록(층 씬이 재로드돼도 유지).
- 상호작용(E): `scripts/interactions/` — interactable(조사), locked_door(열쇠 문), pickup_item(접촉 획득), exit_door(현관 탈출→엔딩). Area2D는 collision_layer 2, prompt_text로 "[E] …" 안내 표시.
- 잉크통(#169): 4층 정보부실에서 얻는 `ink_can`을 **Q**로 바라보는 방향에 던진다(1회용). `scenes/items/ink_projectile.tscn`이 레이캐스트로 벽을 확인하며 날아가다 터지고, 반경 120px 안의 수위를 5초간 멈춘다(`janitor.blind()` — 추적·접촉 판정 정지, 층을 옮기면 해제). 쓸 수 있는 아이템의 조작 키는 `hud.gd`의 `USABLE_ITEM_KEYS`에 적어 R 패널에 표시한다.
- 그리기 순서: **`Floor`(맵 바탕) → `Ground`(복도 마루) → `Rooms`(방 바닥) → `RoomMarks` → `Props`(집기) → `Stairwells` → `Structures`(벽·계단 시각)** — **전부 레이어 0이다**(#307). 그 위에 `WallGlow`(CanvasLayer 1)가 방 이름·상호작용 표시·**문**(#318)을 얹고, `WallFade`(1)가 시야 마스크, 플레이어 시각(2), HUD(3) 순이다. **바닥에 그리는 것(`FM_`, `sc.floor_mark`)과 벽·가구에 붙는 것(`PD_`, `sc.decor`)은 레이어가 다르다** — 화장실 바닥 타일을 장식으로 냈더니 가구 밑에 깔리는 게 정상인데 `verify_props`가 집기와 겹쳤다고 잡았다. 벽면에 붙는 것은 `WD_`(`sc.wall_decor`)로 `Structures` 안에 낸다(걸레받이·기둥).
  **벽·계단 시각은 #307에서 `WallGlow`(CanvasLayer)에서 레이어 0(`Structures`)으로 내려왔다.** CanvasLayer는 별도 캔버스라 `CanvasModulate`(어둠)도 플레이어의 `PointLight2D`(손전등)도 안 받는다 — 그래서 벽·계단 표지가 **시야 밖에서도 100% 밝기**로 보였다. #117에서 "벽은 어둠 속에서도 희미하게 보인다"로 일부러 정했던 것인데, 사용자가 그 판단을 뒤집었다(2026-08-22). 이제 벽은 손전등이 닿는 곳만 보이고 광원 차단체(`LO_`)에 가린 벽은 안 보인다.
  **문(`Door_`·`SDVis_`)만은 다시 `WallGlow/Doors`에 있다(#318, #307을 부분적으로 되돌림).** 방 내부를 숨기는 것(#292)과 복도에서 문의 위치를 아는 것은 다른 문제다 — 벽처럼 손전등이 있어야만 보이면 복도에서 문을 찾을 수 없다. `WallGlow/Doors`는 라벨과 같은 `interact_marks.gd`(반경 420/300)를 써서 어둠과 무관하게 거리로만 나타난다. **문짝·문 표식의 광원 차단체(`LO_*DoorCollision`)는 그대로 층 씬 루트(`SlideDoor_<key>/SDPanel`)에 남는다** — 시각만 옮겼을 뿐 방 내부를 가리는 물리·광원 차단은 그대로다. `WallGlow`는 `follow_viewport_enabled = true`라 CanvasLayer 경계를 넘어도 `to_global()`이 맵 좌표를 그대로 돌려준다(`janitor.gd::_polygon_rect` 참조) — 벽처럼 순찰 루트 계산에 그대로 쓸 수 있는 이유다. 텍스처는 `CanvasItem` 상속 사슬이 CanvasLayer에서 끊기므로 `CANVAS_ITEM_ROOTS`에 `"WallGlow/Doors"`를 넣어 `poly2d()`가 폴리곤마다 `texture_filter`/`texture_repeat`를 직접 적게 했다.
  **`Structures`는 `Stairwells` 뒤에 선언한다** — 앞에 두면 벽이 집기 밑으로 깔린다. 난간·화살표까지 벽과 **같이** 내려야 순서가 맞는다(#234는 문짝만 레이어 0에 뒀다가 `WallGlow`가 덮어 문이 아예 안 보였던 건이다 — #318은 반대로 문만 `WallGlow`에 남기고 벽은 그대로 두므로 같은 함정에 걸리지 않는다: `WallGlow`는 레이어 0 위에 그려지므로 `Structures`의 벽이 문을 가리지 않는다).
  전부 되돌리려면(문 포함) `Structures`를 `WallGlow` 자식으로 되돌리고 `Stairwells` 앞으로 옮기면 된다. 경로를 아는 곳이 셋이다 — `sliding_door.gd`의 `leaf_visual`, `janitor.gd`의 문 시각 조회, `verify_janitor_route.py`. **먼저 선언된 형제가 먼저 그려지므로 선언 순서가 곧 레이어다.** 방 바닥은 `room_floor()`가 종류별로 칠하고(교실 마루·화장실 타일·창고 시멘트…), 막힌 공간은 더 어둡다. 복도는 `add_ground()`가 마루 + 널 이음매를 낸다. **널은 복도를 가로지르는 세로 방향**이고, 선은 폴리곤이 아니라 `floor_board` **무늬**가 낸다(#268). 복도와 같은 방향으로 눕혔더니 선이 끝에서 끝까지 이어져 마루가 아니라 줄 친 종이로 보였고, 직각으로 돌려 폴리곤으로 그렸더니 층당 150개가 넘어 노드 예산을 넘겼다. 무늬로 옮기니 노드 0개에 타일 격자(32px)와도 정확히 맞는다. 화장실 줄눈도 같은 이유로 `floor_matte` 무늬로 옮겼다(#277) — 그래서 `LINELESS`는 이제 비어 있다.
  창문은 짙은 밤하늘색 유리이고, 그 안쪽에 **진짜 광원**(`PointLight2D`)이 선다 — `sc.window_light()`가 `Lights`(씬 루트 Node2D)에 낸다(#274). 폴리곤으로 세 번 실패한 자리다: 불투명한 바닥 표시(#268) → 사다리꼴 → 반투명 사다리꼴(#271). 매번 다른 이유로 빛이 아니라 바닥에 깔린 회색 천으로 보였다. **그리는 방법이 아니라 도구가 틀렸다** — 폴리곤은 가장자리가 안 번지고, 아래를 덮을 뿐 밝히지 못하고, 벽에 안 막힌다.
  - **광원을 `WallGlow`에 두면 안 된다.** CanvasLayer는 별도 캔버스라 그 안의 광원이 아래 레이어의 바닥·집기를 못 비춘다. 반대로 벽(`WallGlow`)은 달빛을 받지 않는데, 벽은 원래 어둠도 안 받으므로 앞뒤가 맞는다.
  - **그림자를 켠다**(`shadow_enabled`). 끄면 벽을 통과해 옆 방까지 밝힌다. 벽에는 이미 `LO_` 차단체가 있다. 광원은 화면 밖이면 컬링되므로 층당 서른 개를 달아도 카메라(1600x900)에 드는 것은 대여섯 개다.
  - 밝기는 `MOON_ENERGY`(1.25)·`MOON_SCALE`(0.72). 어둠이 `(0.1, 0.1, 0.13)`, 손전등이 1.3이라 달빛이 손전등보다 밝아지면 안 된다. **복도에는 광원을 두지 않는다** — 손전등만이 광원이라는 성격(#74)은 복도에서 그대로다.
  - **달빛은 볼 수 있을 때만 켠다**(#292). `Godot 2D에는 시야 판정이 없다` — 광원이 켜져 있으면 그 자리가 화면에 그려지고 플레이어가 어디 있든 보인다. 늘 켜 뒀더니 **닫힌 문 너머 교실 내부가 다 보였다.** 벽·문짝 차단체는 빛이 방 **밖으로 새는 것**만 막지, 방 안이 그려지는 것은 못 막는다.
    방마다 `Lights/Room_<키>` **Area2D**(`scripts/game/room_lights.gd`)로 묶고 평소에는 `visible = false`로 끈다. **안 보이는 Area2D도 몸을 감지한다** — `visible`은 그리기만 끄고 물리는 그대로라, 묶음 자체가 감지기를 겸한다. 켜는 조건은 둘 — **문이 열려 있거나**(`sliding_door.gd`가 `hold()`/`release()`로 알린다) **플레이어가 방 안에 있거나**. 문만 보면 들어와서 문을 닫았을 때 캄캄해지고, 방만 보면 열린 문으로 들여다볼 때 달빛이 안 보인다.
    **`collision_mask = 1`은 벽·집기·문짝 StaticBody2D까지 전부 잡는다.** 그냥 세면 방이 늘 켜져 있다 — #234에서 미닫이문이 영구 개방됐던 것과 같은 함정이다. `CharacterBody2D`만 세고 수위(`janitor` 그룹)는 뺀다(그가 들어왔다고 밝아지면 위치가 새어 나간다).
    **방 감지에 `CollisionPolygon2D`를 쓰면 안 된다** — `verify_props`가 `PropBodies` 밖의 모든 폴리곤 충돌을 벽으로 세고 `verify_floor_reach`가 그것을 막힌 것으로 봐서 방이 통행 불가가 된다. `CollisionShape2D` + `RectangleShape2D`를 쓴다.
  - 창가에서 **E**를 누르면 창밖 묘사가 뜬다(`sc.window_probe()` → `Window_*` Area2D + `interactable.gd`). **방마다 하나만** 둔다 — 창마다 달면 층당 서른 개가 되는데 `_find_interactable`는 겹친 것 중 아무거나 돌려주므로 단서를 가로챈다. 대사는 `WINDOW_LINES`에 층마다 셋, 방 이름으로 골라 재생성해도 같은 방에 같은 대사가 붙는다.
  - 창가 조사는 **벽에 붙는 것이 정상**이라 `verify_props`의 `CLUE_CLEAR` 검사에서 뺀다. 대신 `verify_floor_reach`가 도달 격자로 실제로 다가갈 수 있는지 본다. 그 검사 때문에 교실에 **창가 통로**(`CLASS_WIN_LANE`)가 생겼고 세로 통로가 문에서 창까지 통째로 이어지게 됐다 — 예전에는 창가가 책상에 둘러싸인 주머니였다(열 사이 틈 20px < 플레이어 지름).
  **복도 바닥에는 아무것도 놓지 않는다**(#271, #274, #277). 바닥 유도선 → 천장 배관 → 꺼진 형광등(190x16) → 닳은 자국(`Worn_` 279x26)·물기·종이 → 스프링클러 헤드(9x9) 순으로 **다섯 번 넣었다가 다섯 번 걷어냈다.** 위에서 내려다보는 화면에는 높이가 없어서 **천장에 달렸다는 사실을 그림만으로 전할 방법이 없고**, 바닥에 깐 것은 무엇이든 떨어진 물건으로 보인다. 복도를 채우는 것은 **벽에 붙는 것**(사물함·게시판·소화전·걸레받이)뿐이다.
  `add_corridor_floor_marks()`·`add_ceiling_lights()`·`_corridor_clear()`는 그래서 없어졌다. 되살리고 싶어지면 위 다섯 번을 먼저 읽을 것.
  층당 노드는 **4500개** 안쪽으로 둔다(#301에서 3300 → 4500). **처음으로 실측해서 정한 값이다** — 3000도 3300도 재 보지 않고 잡은 숫자였다.

  | 항목 | 실측 (4층 3266노드, GTX 1660 SUPER, vsync 끔) |
  |---|---|
  | 프레임 | **0.87 ms → 1144 fps** |
  | 씬 로드 | 71 ms |
  | 인스턴스 | 11 ms |
  | 트리 추가 | 20 ms |

  병목은 프레임이 아니라 **층 전환 100ms**이고 그건 페이드로 가려진다. 노드를 400개 더 넣어도(#301) 프레임은 0.86 → 0.95ms로 그대로였다. 현재 최대는 4층 3343개다. **또 올릴 일이 생기면 이 표를 다시 재서 갱신할 것** — 근거 없는 숫자로 되돌아가지 않게.
- **높이 단서가 없으면 물건으로 안 읽힌다**(#289). 이 프로젝트의 성공·실패가 전부 이 규칙 하나로 정리된다 — 바닥에 납작한 폴리곤을 깐 다섯 번(유도선·배관·형광등·닳은 자국·스프링클러)은 전부 실패했고, 벽 3단(#268·#271)·무늬로 옮기기(#268·#277·#283)·진짜 광원(#274)은 성공했다. 그래서 집기 그림에도 같은 것을 넣는다.
  - `gen_tiles.py`의 `solid_obj()`가 오브젝트 13종을 감싸 **윗변 하이라이트 / 아랫변 앞면 / 접지 그늘 / 오른쪽 옆면**을 준다. 예전에는 `_edge()`가 사방 1도트를 똑같이 어둡게 칠할 뿐이라 어느 쪽이 위인지 알 수 없었다. **노드는 하나도 안 는다** — 집기마다 그림자 폴리곤을 내면 층당 600개가 늘어 예산을 넘긴다. 대신 그림자가 물건 **바깥**으로 못 나가므로 드리운 그림자가 아니라 앞면과 접지 그늘이 된다.
  - `shade_jitter()`가 집기마다 색을 ±6% 흔든다 — 교실 책상 스무 개가 완전히 같은 색이라 복사·붙여넣기로 보였다. **텍스처를 고른 뒤에** 걸어야 한다(`SPRITE`·`TEX`가 색 문자열을 키로 쓴다). 벽·바닥에는 걸지 않는다 — 이어붙는 면이라 조각마다 색이 다르면 누더기가 된다.
- 집기 위 작은 소품은 `add_clutter()`가 **이미 놓인 집기를 훑어** 얹는다(#289). 방마다 손으로 놓지 않는다. 종류·자리·있고 없고를 집기 이름 해시로 정하므로 재생성해도 같은 자리에 같은 것이 온다. 대응표는 `CLUTTER`(집기 색 → 소품 목록)와 `CLUTTER_CHANCE`(붙을 확률). 전부 `PT_`라 충돌이 없고 집기 안에 들어가므로 `verify_props` 규칙을 자동으로 지킨다. **바닥에 흩뿌리지 않는다** — 물건은 물건 위에 놓는다.
- 방·복도 집기: 방이 빈 사각형이라 들어갈 이유가 없던 것을 `gen_floors.py`의 `add_props()`가 채운다. 노드는 세 갈래다 — 충돌 `PC_`(StaticBody2D `PropBodies`) + 시각 `PV_`(`Props`) 한 쌍, 충돌 없이 벽에 붙는 장식 `PD_`(칠판·게시판·창문·시계·소화기), 집기 위에 놓이는 소품 `PT_`(책상 위 교과서·선반의 책). `PD_`는 집기와 겹치면 안 되고 `PT_`는 반대로 어느 집기 안에 온전히 들어가야 한다 — `verify_props`가 각각 확인한다. **벽과 달리 광원 차단체를 달지 않는다**(낮은 집기 설정 + 조명 튜닝 #74를 흔들지 않으려는 목적)—그래서 `verify_scenes`의 벽↔차단체 1:1 검사에 걸리지 않는다.
  - 교실은 전용 배치 `_classroom()` — **가로 배치**다. 칠판은 왼쪽 옆벽, 교탁은 그 앞, 학생은 왼쪽을 보고 앉는다(의자가 책상 오른쪽). 오른쪽 옆벽에 청소도구함·사물함·쓰레기통, 문 반대쪽 외벽에 창문, 문 쪽 벽에 게시판. 세로 배치였을 때는 복도와 맞닿은 벽에 칠판이 가고 외벽에 창문이 안 붙어 방향이 거꾸로였다.
    사물함·청소도구함·쓰레기통은 **오른쪽 벽면(`wr`)에 붙인다**(#277). 예전에는 `ix1`(벽에서 `CLASS_EDGE`=24 안쪽)에 세워 벽에서 떠 있었다. 시계는 그 자리를 비우려고 앞쪽 벽(칠판 위)으로 옮겼다 — 실제 교실에서도 시계는 칠판 쪽에 있다.
    **통로 세 갈래를 먼저 잡고 남는 자리에 책상을 놓는다.** ① 가로 통로는 방 중심(`verify_floor_reach`가 보는 곳), ② 창가 통로(`CLASS_WIN_LANE`)는 창가 조사에 다가가려고, ③ 세로 통로는 문에서 창까지. ③은 문 쪽 절반에서 **cx에 맞춰 열 하나를 통째로 뺀다**(#277) — 예전처럼 `door_lane`(72px)에 걸리는 열을 지우면 통로 폭이 열 간격(63px)보다 넓어 늘 두 열이 사라졌다. 창 쪽 절반은 열을 지우는 대신 **열 사이 한 자리를 넓힌다**(#274).
  - 교무실·부서실은 전용 배치 `_office()`(#277). 책상 둘을 마주 붙이고 사이에 칸막이를 세운 **섬**을 격자로 놓고, 창가 쪽 남는 띠에 부장 책상, 옆벽 자투리에 서류 캐비닛·회의 탁자·소파·복사기를 `_stack()`으로 쌓는다. 열은 **짝수**여야 문 앞(cx)이 비고, 열 수는 옆벽에 `OFFICE_SIDE`(150)를 남기고 정한다 — 폭을 전부 책상으로 채우면 회의 탁자 자리가 없어 다시 책상만 늘어선 방이 된다. 섬이 단서 여유에 걸리면 **반쪽(한 자리)이라도** 넣는다.
    단서 여유는 `OFFICE_CLUE_CLEAR`(62)로 공용 값(76)보다 좁다. `verify_props`는 30이면 되지만 **은신처는 걸어가 닿아야 하므로**(`verify_hiding_spots`) 통로 폭(46)보다 넉넉해야 한다 — 40으로 줄였더니 체육건강부·진로진학부 은신처가 집기에 막혔다.
    창문·달빛·창가 조사는 교실과 같은 `_windows()`를 쓴다. 부서실도 북쪽 외벽에 붙어 있어 창이 있어야 맞는데 #274에서는 `_classroom()`에만 넣어 빠져 있었다.
    통로가 둘이다. **가로 통로가 방 중심을 지난다** — 막히면 `verify_floor_reach`가 방을 도달 불가로 본다. **세로 통로**(`CLASS_DOOR_LANE`, 72px)는 문에서 가로 통로까지 잇는다. 문 틈(110) 폭으로 비우면 북쪽 교실 책상 열 3개가 전부 걸려 문 쪽 절반이 통째로 빈다.
    뒷벽 사물함은 복도용보다 좁은 `CLASS_LOCKER_W`/`CLASS_BACK_PAD`를 쓴다. 벽 장식은 `_spread()`가 방 폭에 맞춰 개수를 줄이고, 그래도 안 되면 폭을 줄여 한 장이라도 건다.
    벽에서 `CLASS_EDGE`(24px)만큼이 집기 자리고 **그 바깥 벽면 띠는 장식 전용**이다. 태극기·칠판·분필받이·TV(왼쪽), 시계(오른쪽), 창문+커튼(외벽), 스피커·게시판(문 쪽)이 여기 붙어 통행에 전혀 영향을 주지 않는다. 장식 앵커는 벽면 방향을 따라야 한다 — 두께를 한 방향으로만 재면 반대쪽 벽에서 벽을 파고든다(스피커에서 실제로 났다).
  - **교실 문은 한 짝짜리 미닫이문**이다(`add_sliding_doors()` + `scripts/interactions/sliding_door.gd`). **플레이어는 E로 여닫고(`[E] 문 열기`/`문 닫기`), 수위는 E 없이 지나간다.** 그래서 루트가 Area2D이고 스크립트가 거기 붙는다 — 플레이어의 `InteractionArea`는 겹치는 Area2D 중 `interact()`를 가진 것을 찾아 `prompt_text`를 읽는다. `collision_layer = 2`(플레이어가 찾는 층) + `collision_mask = 1`(수위 몸 감지)를 함께 쓰고, 수위는 `"janitor"` 그룹으로 가린다. **수위가 문에 막히면 추격이 깨진다** — `SDPanel*`이 경로탐색에서 늘 열린 것으로 취급되므로 격자와 실제가 어긋난다. 패널 폴리곤은 **절대 좌표**로 낸다 — 검사 스크립트들이 노드 position을 반영하지 않고 폴리곤을 그대로 읽는다.
    **`SDPanel*` StaticBody2D는 막힌 것으로 세지 않는다** — 다가오면 열리므로. 이 예외가 `janitor.gd::_collect_blockers`, `verify_floor_reach.py`, `verify_janitor_route.py`, `verify_hiding_spots.py` **네 곳**에 같은 이름 규칙으로 들어 있다. 이름을 바꾸면 네 곳을 함께 고쳐야 한다.
    **문짝에는 광원 차단체가 붙는다(#256 — #225의 반대 결정).** 없을 때는 문 틈이 벽 없는 구간이라 닫힌 문 너머가 훤히 보여서 문이 있으나 마나였다. 차단체는 **문짝 몸체의 자식**이라 문과 함께 움직인다 — 열리면 벽 쪽으로 비켜나 그 자리 벽 차단체와 겹치고 문 틈이 빈다. 그래서 여닫이에 맞춰 켜고 끄는 코드가 없다. 충돌 노드 이름을 `<방키>DoorCollision`으로 두어 **`verify_scenes`의 벽↔차단체 1:1 검사에 편입**시켰다(누락·폴리곤 불일치를 검사기가 잡는다). 그래서 충돌 두께는 벽 전체다 — `DOOR_LEAF_T`로 끼워 두면 위아래 3px씩 틈으로 빛이 샌다. 노드 이름이 방마다 다르므로 `sliding_door.gd`는 이름이 아니라 **타입으로** 충돌을 찾는다.
  - 그 밖의 방은 `prop_kind()`로 종류를 정하고 `PROP_SPECS`의 단위를 깐다. 단위 크기는 최대치라 좁은 자리에서는 `PROP_MIN_W/H`까지 줄어든다(규격을 고집하면 화장실·현관이 통째로 빈다).
  - **화장실 배치(#262·#265)**: 왼쪽 벽 세면대+거울, **오른쪽 벽 대변기 칸**(세로로 쌓이고 왼쪽으로 열린다), 문 반대쪽 벽 소변기(남자)·청소도구, 문 쪽 벽 환풍기. 자리가 겹치는 것들이라 **놓는 순서가 곧 우선순위다** — 칸이 오른쪽 벽을 먼저 잡고, 구석 집기(휴지통)는 화장실에서만 왼쪽 벽으로 제한한다(오른쪽 구석을 차지하면 칸이 통째로 취소돼 26곳 중 13곳에 칸이 없었다).
  - `_toilet_cubicles()`가 칸막이 세 면 + 열린 앞면으로 만들고 안에 변기를 놓는다. **실제로 걸어 들어갈 수 있다** — 크기 기준은 플레이어 스프라이트(60×72)가 아니라 **충돌 캡슐(반경 8·높이 26)** 이다. 변기가 칸을 다 채우면 안 된다: 도달성 격자가 장애물을 플레이어 반경(10)만큼 부풀리므로 변기 앞에 40px은 남아야 한다(48×38짜리를 넣었더니 39칸 전부 들어갈 수 없었다). 칸 깊이는 방 안쪽 높이의 `CUB_DEPTH_RATIO`(0.46)로 조인다 — 더 깊으면 칸 앞면이 방 중심에 걸려 `verify_floor_reach`가 방을 도달 불가로 본다. 옆 칸막이는 뒷벽 안쪽 면까지만 온다(모서리에서 겹치면 `verify_props`가 잡는다).
  - 방 종류별 부속은 `add_room_fixtures()`가 붙인다 — 단위 위 소품(`PROP_OVERLAY`: 모니터·서류·개수대), 단위 아래 의자(`PROP_COMPANION`), 문 반대쪽 벽 장식(`WALL_DECOR`: 화이트보드·안내판·열쇠판), 문에서 먼 구석 집기(`CORNER_PROP`: 서버랙·약품장·침상·화분), 화장실 세면대+거울. 구석 집기는 좌·우 벽 × 먼쪽·가까운쪽 네 자리를 시도한다 — 수위실은 단서 4개가 가운데 띠를 차지해 한 자리만 보면 늘 막혔다. **사선 방은 벽 장식을 건너뛴다**(cx 한 점에서 잰 값으로 기운 벽을 가정하면 뚫는다).
  - 복도는 `add_corridor_props()`가 맞닿은 방 벽마다 사물함을 붙이고 남는 벽면에 소화기·게시판을 건다. 소화전 함과 정수기는 **사물함보다 먼저** 자리를 잡는다 — 자투리 벽면을 기다리면 사물함이 벽을 다 먹어서 한 개도 안 걸린다. 복도 띠는 빌더가 넘긴다(2~5층 3개, 1층 1개). 문 틈 양옆 `CORR_DOOR_PAD`는 비운다 — 수위 순찰의 문 앞 대기 지점이 거기다. 벽면이 복도 위/아래 어느 쪽인지에 따라 장식 앵커가 뒤집히므로 `face()`로 두께마다 맞춘다.
- 플레이어 스프라이트(#210): 원본 캐릭터 아트 `assets/sprites/source/player_design.png`(대기 정면 + 달리기 측면 두 포즈)에서 **`tools/gen_player_sprites.py`가 60×72 도트 스프라이트를 잘라 낸다**(표준 라이브러리만, 결정론적). 크기를 바꾸려면 그 스크립트의 `CANVAS_W`/`CANVAS_H`를 고치고 다시 돌린 뒤 `player_controller.gd`의 `SPRITE_OFFSET_Y`(발끝을 충돌 캡슐 바닥에 맞추는 값)도 같이 맞춘다. 포즈 전환·좌우 반전은 `player_controller.gd`의 `_update_sprite()` — **사람 그림이라 이동 각도로 회전시키지 않는다**.
- 수위 스프라이트(#310): 원본 열 장(`assets/sprites/source/janitor/`)에서 **`tools/gen_janitor_sprites.py`가 48×80 칸 3열×4행 시트 `assets/sprites/janitor_sheet.png`를 굽는다**(표준 라이브러리만, 정수 연산만 써서 결정론적). 열=걸음, 행=방향이고 행 순서는 `janitor.gd`의 `ROW_DOWN/LEFT/RIGHT/UP`과 맞아야 한다. **예전 `Polygon2D` 오각형은 이동 각도로 회전했는데 그 회전을 걷어냈다** — 사람 그림은 회전시키지 않고 행을 바꾼다(플레이어와 같은 규약). 걸음 프레임은 시간이 아니라 **이동 거리**(`WALK_STRIDE`)로 굴린다: 순찰(130)과 추격(290)에서 따로 맞추지 않아도 발이 미끄러지지 않는다.
  - 원본은 인물이 캔버스를 세로로 꽉 채우므로 **캔버스째 줄이면 발 높이가 저절로 맞는다.** 인물 bbox에 맞춰 줄이면 발을 든 걸음 프레임에서 키가 늘어난다.
  - 배경은 불투명한 흰색이다. **가슴 명찰·손전등 렌즈도 희므로** 흰색을 통째로 지우면 안 되고, 테두리에서 시작하는 flood fill로 바깥과 이어진 흰색만 지운다.
  - 축소는 평균이 아니라 **최빈색**이다 — 평균을 내면 원본에 없던 중간색이 생겨 도트 아트가 흐려진다.
  - **뒷모습(위) 원본은 한 장뿐이고** 다른 아홉 장과 렌더가 다르다(1024, 보간됨). 세 열이 같은 그림이라 위로 걸을 때만 1px 흔들어(`UP_BOB_STRIDE`) 걸음을 만든다. 위 걷기 그림이 생기면 `gen_janitor_sprites.py`의 `ROWS`만 고치면 된다.
  - 잉크(#169)를 맞으면 색을 갈아 끼우는 대신 `modulate`(`BLIND_MODULATE`)로 죽인다 — 그림이 있으므로 단색으로 덮으면 실루엣만 남는다.
  - 플레이어 시각은 `CanvasLayer`(layer 2)에 있지만 **수위 `Body`는 레이어 0에 둔다**(#307) — 어둠과 손전등을 받아야 시야 밖에서 안 보인다.
- 계단 열쇠 스프라이트(#333): 원본 `assets/sprites/source/key_design.png`(흰 배경 위 황금 열쇠)에서 **`tools/gen_key_sprite.py`가 24px 도트 스프라이트 `assets/sprites/key.png`를 굽는다**(표준 라이브러리만, 정수 연산만 써서 결정론적). 쓰는 곳은 `story_objects.json`의 열쇠 시각 노드 6개(1층 창고·2층 배수구·3층 고리·4층 다산7실/컴퓨터실·5층 미술창고)이고, ext_resource는 `gen_floors.py`의 `SPRITE_TEXTURES`가 선언한다 — 타일 무늬가 아니라 그림 한 장이라 `TEX`/`SPRITE` 표와 경로가 다르다(`assets/sprites`).
  - 인물 스프라이트와 달리 **배경을 지운 뒤 경계상자로 자른다.** 인물 원본은 캔버스를 세로로 꽉 채워 캔버스째 줄이면 발 높이가 맞았지만, 열쇠는 캔버스 한가운데 여백을 두고 있어 그대로 줄이면 24px 중 열쇠가 몇 px뿐이다.
  - **고리 구멍은 따로 뚫는다**(`punch_holes`). 테두리 flood fill은 바깥과 이어진 흰색만 지우므로 윤곽선에 둘러싸인 구멍이 막힌 채 남아 열쇠가 숟가락처럼 보였다. 몸통 하이라이트도 거의 흰색이라 **넓이로 가른다** — 실측(1254² 원본) 구멍 17862px vs 다음으로 큰 하이라이트 4951px이라 `HOLE_DEN`(캔버스의 0.5%)로 자르면 양쪽에 두 배 여유가 있다.
  - 덮임 문턱은 `COVER` 2/5로 인물(1/2)보다 낮다 — 대각선으로 누운 열쇠는 축과 어긋난 가장자리·톱니가 칸을 반쯤만 채워서 1/2로 자르면 실루엣이 끊긴다.
  - 크기는 `MAX_SIDE`(24, 긴 변). 예전 폴리곤 자리(22x10)와 맞고 고리 구멍·톱니 한 개가 살아남는 최소치다. 스프라이트는 계속 **레이어 0**(pickup Area2D의 자식)이라 어둠·손전등을 받는다 — 시야 밖에서 떠 있으면 안 된다(#307). 찾는 것은 호박색 상호작용 표시(#301)가 맡는다.
- 도트 에셋은 **재질과 오브젝트 두 갈래**다(#259). `tools/gen_tiles.py`가 둘 다 굽는다. **재질**(`PATTERNS`, 바닥·벽)은 `uv`를 비워 정점 좌표를 UV로 쓰므로 맵 전체가 한 격자에 정렬되고 이어붙는다. **오브젝트**(`OBJECTS`, 책상·사물함·세면대…)는 `gen_floors`가 물건마다 **로컬 UV**를 넣어 그림 한 장을 그 물건 경계상자에 맞춰 늘린다 — 44×26 책상이든 140×38 실험대든 상판·모서리·서랍선이 제자리에 온다. 재질만 쓰던 때는 월드 격자의 아무 조각이나 잘려 들어갔다. 대응표는 `gen_floors.py`의 `TEX`(재질)와 `SPRITE`(오브젝트)이고 **`SPRITE`가 우선**한다. 오브젝트는 이어붙지 않아도 되므로 `_check_periodic`을 건너뛰되 평균 휘도·명암·도트 격자 규약은 재질과 똑같이 지킨다. `SPRITE_SIZE`(32)는 `gen_tiles.py`의 `OBJ`와 같아야 한다.
- 도트 타일 텍스처(#246, 원안 #243): 바닥·벽·집기 `Polygon2D`는 단색이 아니라 **`tools/gen_tiles.py`가 구운 회색조 도트 무늬**(`assets/tiles/*.png`)를 물고 있다(표준 라이브러리만, 결정론적). 무늬만 텍스처이고 **색은 `gen_floors.py`의 팔레트가 계속 쥔다** — 방 종류별 바닥색(#237)과 #242의 마루 톤이 그대로 살아 있게. 색 → 타일 대응은 `gen_floors.py`의 `TEX` 표이고, 여기 없는 색은 단색으로 남는다(열쇠·화살표·천장등 같은 표식, 그리고 널 이음매·화장실 줄눈·칸막이문처럼 2~5px로 얇은 것). 타일 한 변은 바닥 **256**(`FLOOR`), 벽·얇은 장식 **16**(`SMALL`)이다. 바닥 무늬의 **격자 칸**은 여전히 32px(`FLOOR_CELL`) — 널 폭·줄눈 간격·장 크기가 그것이고, 한 장에 8x8칸이 들어간다(#283). 타일 한 장이 곧 한 칸이던 때는 담을 수 있는 것이 잔 결뿐이라 얼룩을 넣는 순간 32px마다 되풀이되는 벽지가 됐다. 무늬 함수는 칸 크기로 `gw`가 아니라 **`SUB`**(칸의 도트 수)를 써야 한다 — `gw`를 칸으로 쓰면 타일을 키울 때 널이 같이 넓어진다. 128도 해 봤는데 화면 폭(월드 1280px)에 열 번 반복돼 되풀이가 보였다. **면 두께보다 큰 타일을 쓰면 안 된다** — UV가 월드 좌표라 얇은 면에는 타일의 일부만, 그것도 면의 y에 따라 다른 조각이 잘려 들어간다. #259에서 벽을 32로 올렸다가 벽돌 줄눈이 벽 가장자리(T=16)에 안 맞아 #262에서 되돌렸다. 두께 8~10px인 게시판·창문·걸레받이도 같은 이유로 16을 쓴다. 무늬 함수의 칸 크기(마루 널 높이·장 크기 등)는 `32//DOT = 16`으로 나누어떨어져야 이어붙는다 — 안 그러면 `_check_periodic`이 잡는다. 규약 넷: ① 무늬 최소 단위는 월드 2px(`DOT`) — 카메라 zoom이 1.25라 1px 무늬는 화면에서 들쭉날쭉해진다. ② 모든 타일의 평균 휘도는 `MEAN`(0.78)로 고정하고 `gen_floors`가 텍스처를 붙인 색에만 `1/TEX_MEAN` 게인을 준다 — 그래서 평균 밝기가 텍스처 도입 전과 같고 조명 튜닝(#74)이 흔들리지 않는다(두 상수는 같이 고친다). ③ UV를 비워 정점 좌표를 UV로 쓰므로 맵 전체가 하나의 타일 격자에 정렬된다. `texture_filter = 1`(Nearest)·`texture_repeat = 2`는 층 씬 루트와 `WallGlow/RoomWallVisuals`에만 걸고 자식이 물려받는다 — **CanvasLayer 직속 노드(계단 난간)만 예외로 직접 적는다**(물려받을 부모가 없다). ④ **바닥 디테일은 폴리곤이 아니라 무늬로 낸다** — 복도 마루널(#268)도 화장실 줄눈(#277)도 예전에는 `FM_` 폴리곤이었는데, 타일 주기와 어긋나 격자가 겹쳐 보이는 데다 층당 수십~수백 개의 얇고 긴 막대가 바닥에 누웠다. 지금은 `floor_board`/`floor_matte` 무늬가 32px 주기로 내므로 노드 0개에 타일 격자와 정확히 맞는다. `LINELESS`·`_check_lineless`는 그래서 비어 있지만, 새 바닥 무늬를 더할 때 되살릴 수 있게 남겨 뒀다. 닳은 자국·번진 때도 같은 길을 갔다(#283) — `wear()`/`blot()`이 타일을 감싸는 값 잡음으로 낸다. **자리마다 다른 흔적은 무늬로 낼 수 없다**(타일은 반복된다). 그러려면 폴리곤으로 돌아가야 하는데 그 길은 다섯 번 실패했다. 새 타일을 넣으면 `TEX`에도 넣어야 실제로 쓰이고, 반대로 `TEX`에 없는 타일은 굽지 않는다.
- 상호작용 표시(#301): 상호작용 자리마다 `WallGlow/Marks` 아래 마름모를 하나 낸다(`sc.mark()`). **가까이 가야 보인다** — `scripts/game/interact_marks.gd`가 플레이어와의 거리로 밝기를 매긴다(210px에서 뜨기 시작해 120px에서 최대). 온 맵에 늘 떠 있으면 탐색이 사라지고 닫힌 문 너머까지 드러나 #292를 되돌린다.
  - **어둠을 받으면 안 되므로 CanvasLayer(`WallGlow`) 안에 둔다.** 레이어 0에 두면 `CanvasModulate`(0.1)에 곱해져 손전등이 정확히 그 위를 비출 때만 보인다.
  - 색이 곧 종류다 — **호박색**(`C_MARK_KEY`) 진행에 필요한 것(단서·열쇠·잠긴 문), **청록**(`C_MARK_HIDE`) 은신처, **흐린 회청**(`C_MARK_FLAVOR`) 그 밖의 조사. 미닫이문에는 표시가 없다(문은 이미 문처럼 생겼다).
  - 표시는 CanvasLayer 안이라 상호작용 Area2D의 **자식이 될 수 없다.** 주운 아이템·열린 문이 사라지면 표시만 남으므로 이름 규약 `Mark_<임자>`로 임자를 찾아 없어졌으면 끈다 — 그쪽 스크립트를 안 건드려도 된다. 조사한 단서는 `interactable.gd`의 `investigated`를 보고 흐려진다(지워 버리면 뭘 봤는지 되짚을 수 없다).
  - **폴리곤을 원점 기준으로 내고 자리는 `position`에 준다.** 다른 노드처럼 절대 좌표로 내면 노드의 `global_position`이 (0,0)이라 거리 계산이 전부 빗나가 표시가 하나도 안 켜진다(실제로 그랬다).
- 집기 조사(#301, 문구는 #304에서 다시 씀): `add_examine()`이 `add_clutter()`와 같은 방식으로 **이미 놓인 집기를 훑어** 조사 대상(`Exam_*`)을 붙인다. 진행에는 영향이 없다(플래그·아이템 없음).
  - 문구는 `EXAMINE_LINES`를 **(집기 색, 방 종류)** 로 찾는다 — 방 종류가 없는 항목이 기본값이다. 교무실 책상이 교실 책상과 같은 말을 하면 방이 어디든 똑같아 보인다.
  - **방마다 `EXAMINE_PER_ROOM`(2)개까지**, 그리고 **한 방에서 같은 문구를 두 번 쓰지 않는다.** 확률로만 거르던 때는 층당 71개가 되고 한 교실 안에서 같은 말이 두세 번 나왔다. 지금은 층당 45개에 서로 다른 문구 33개다.
  - 집기를 이름 해시로 정렬해 고르므로 재생성해도 같은 방에서 같은 집기가 뽑힌다.
  - **`verify_props`의 `CLUE_CLEAR` 검사에서 뺀다**(`Window_`와 같이). 집기 위에 있는 것이 정상이다 — 그 집기를 조사하라고 붙인 것이라 떨어져 있으면 오히려 이상하다.
- 상호작용 우선순위(#301): `_find_interactable()`이 **우선순위 → 거리** 순으로 고른다. 예전에는 겹친 것 중 첫 번째를 돌려줬는데 그 순서가 노드 선언 순이라 사실상 임의였다. 잠긴 문 20 / 단서 15 / 은신처 12 / 미닫이문 4 / 집기 조사 3. 조사 대상을 늘리기 **전에** 이걸 먼저 넣어야 한다 — 안 그러면 책상이 단서를 가로챈다.
- 컷신 스탠딩(#280): 화자 그림은 `subtitle_dialogue.gd`의 `PORTRAITS` 표(화자 이름 → 텍스처)에 있고, `assets/portraits/`에 둔다. **컷신에서만 켠다** — `portraits_enabled`를 `intro.tscn`·`ending.tscn` 인스턴스에서 true로 준다. 본편 HUD 알림(#193)은 같은 씬을 쓰므로 켜면 사람 그림이 게임 화면을 가린다(`shade_alpha`와 같은 방식).
  - **말하지 않는 동안에는 끄지 않고 어둡게** 둔다(`PORTRAIT_DIM`). 지문이 대사 사이에 자주 끼는 프롤로그에서 껐다 켜면 한 장면 안에서 그림이 깜빡인다. 장면이 바뀌면(`clear()`) 없앤다.
  - 스탠딩은 화면 **오른쪽**에 선다(#286) — 이설이 살짝 왼쪽을 보고 있어 오른쪽에 서야 화면 안쪽을 향한다. 그만큼 자막 **오른쪽** 여백을 `PORTRAIT_TEXT_MARGIN`까지 민다. **화자에 따라 여백을 움직이면 글이 좌우로 튀므로, 컷신에서는 그림이 없어도 늘 비운다.**
  - 원본 그림은 투명 여백이 넓다 — **알파 경계상자로 잘라서** 넣는다(1038x1515 → 644x1452). 여백을 남기면 배치 좌표가 그림과 어긋난다.
  - 타일과 달리 `texture_filter`를 Nearest로 두지 않는다. 도트로 보이지만 실제로는 보간된 그림이라(가로 런 길이가 1px에 몰려 있다) Nearest로 축소하면 도트 크기가 들쭉날쭉해진다.
- 조명: main의 `CanvasModulate`(**완전한 검정**, #307) + 플레이어 `PointLight2D`(shadow_enabled) — 벽 차단체 때문에 벽 너머는 보이지 않음. 문·창문 틈으로만 빛이 샘. 예전 `(0.1, 0.1, 0.13)`은 손전등이 안 닿는 바닥·집기를 10% 밝기로 남겨 어둠 속에서도 형체가 보였다.
- **시야 밖은 검다**(#117, 반경은 #304에서 맞춤). `WallFade`(CanvasLayer 1) 안의 `wall_fade_mask.gd`가 가운데는 투명하고 바깥은 검은 원형 마스크를 플레이어에 따라다니게 한다. `Background`보다 **뒤에** 선언돼 있어 층 씬의 `WallGlow`(layer 1)와 레이어 0을 **모두** 덮는다 — 어둠을 안 받는 벽·방 이름·문짝도 이걸로 가려진다.
  - 반경 = **512(텍스처 반지름) x 스프라이트 `scale`**. 지금은 scale 2라 1024px이고, 그라디언트가 195px까지 투명 / **389px에서 완전히 검다**. 손전등 반경(256 x 1.3 = 333px)에 맞춘 값이다.
  - 예전에는 scale 7이었다 — 반경 3584px에 609px에서야 검어져서 **화면(반폭 640px, 반높이 360px) 안이 거의 다 투명했다.** 가리는 장치가 있는데도 학교 도면이 다 보였던 이유다.
  - **스프라이트가 화면 대각선 반지름(734px)보다 커야 한다** — 작으면 화면 네 모서리가 마스크 밖이라 오히려 뚫린다. 그래서 1024 밑으로는 내리지 않는다.
  - 상호작용 표시(#301)는 210px에서 뜨므로 투명 구간 안이다. 길을 잃기 쉬워지는 것은 의도된 대가다 — 되돌리려면 `scale`만 올리면 된다.
  - **방 이름도 거리로 껐다 켠다**(#307). 벽이 캄캄해졌는데 라벨만 허공에 떠 있으면 더 이상하다. `WallGlow/Labels`에 `interact_marks.gd`를 붙이되 반경을 키운다(300px 완전 / 420px부터) — 어느 방인지 알고 다가갈 수 있어야 한다. 같은 스크립트가 표시와 라벨을 함께 다루므로 `Mark_` 앞머리가 없는 자식은 임자 확인을 건너뛰고, 라벨은 `Control`이라 중심을 `get_global_rect()`로 잡는다.
  - **문도 같은 이유로 거리 기반이다**(#318). `WallGlow/Doors`에 라벨과 같은 반경(300/420)의 `interact_marks.gd`를 붙였다 — 방 내부(#292)는 계속 숨기되, 복도에서 문의 위치는 손전등 없이도 알 수 있어야 한다.
- UI: R 인벤토리 패널(5슬롯), 좌상단 HUD(목표/소지품)+층 표시, 하단 알림(game_state.request_notice).
- 수위 접근 방향 표시(#327): `HUD/Root/JanitorAlert`(`scripts/ui/janitor_alert.gd`)가 들리는 거리(720px, `EARSHOT`과 같은 값) 안에서 **화면 테두리 중 수위가 있는 쪽에 쐐기**를 띄운다. 가까울수록 진하고, 추격 중에는 붉게 맥동한다.
  - 소리 단서(#9)는 좌우 감쇠만 주고 **위아래를 구분 못 한다.** #307로 시야 밖을 완전히 가린 뒤로는 수위를 볼 수 있는 거리가 389px뿐이라, 소리만으로는 어느 복도로 도망칠지 고를 수 없었다.
  - **시야를 대신하지 않는다** — 위치는 안 보여 주고 방향만 알린다. 거리는 진하기로만 전한다.
  - 층을 옮기면 같은 수위 노드를 재사용하므로 `my_floor != player_floor`이거나 물리 처리가 꺼져 있으면(4층 안전 구간) 띄우지 않는다.
  - 그라디언트에 셰이더를 쓰지 않는다 — `draw_polygon()`이 정점마다 색을 받으므로 테두리 쪽을 진하게, 안쪽을 투명하게 준다.
- 사운드(#9): 에셋을 받아오지 않고 **`tools/gen_sfx.py`가 8비트 톤으로 합성**해 `assets/audio/*.wav`로 커밋한다(표준 라이브러리만, 고정 시드라 재생성해도 바이트가 같다). 톤을 바꾸려면 그 스크립트의 `build_all()` 숫자를 고치고 다시 돌린다. 비위치 효과음은 autoload `Sfx`(`scripts/game/sound_manager.gd`)의 `Sfx.play(&"id")`, 위치가 정보인 소리(수위 발소리·열쇠·문)는 `janitor.tscn`의 AudioStreamPlayer2D가 낸다. **하단 알림 텍스트는 소리와 병행**한다 — 소리를 못 듣는 상황에서도 단서가 남아야 한다. 오디오 버스는 Master 하나뿐이고 음량은 `sound_manager.gd`의 `VOLUMES`에서 맞춘다.
- 앰비언트·추격 BGM(#176): `tools/gen_music.py`가 만든다(루프라 위상을 루프 길이에 맞춰 고정하고, 이음매 불연속을 자체 검사한다). 본편 진입에 `Sfx.start_music()`, 체포·탈출에 `Sfx.stop_music()`. 수위가 `Sfx.set_chasing()`으로 매 프레임 추격 여부를 알리면 추격 BGM이 페이드 인하고 앰비언트가 낮아진다. **해제는 2.5초 미룬다** — `lose_sight_seconds`(1.5)보다 길어야 모퉁이에서 음악이 깜빡이지 않는다. 루프 지점은 `.import`가 아니라 런타임에 정한다(그 파일은 커밋에 빠질 수 있다).
- 은신처(#204·#324): `EXTRA_HIDING`(층별 손배치) + `story_objects.json`의 것으로 **1~3층 층당 9곳**이다. **종류마다 색·크기가 다르다**(`HIDE_KINDS`) — 예전에는 전부 `C_LOCKER` 36x52라 약품장도 장비함도 화장실 칸도 같은 회색 사물함으로 보였다.
  - 방 안 자리는 **그 방에 몇 개가 들어가는지로** 정한다. 층 전체 목록의 순번으로 나누면 층에 은신처를 더할 때 **이미 있던 것까지 자리가 밀려** 집기에 막힌다(`verify_hiding_spots`가 계단에서 걸어 닿는지 본다).
  - 교무실·부서실은 책상 섬이 방 가운데를 차지해 자동 배치가 막힌다 — `HIDE_POS`에 자리를 손으로 적는다.
  - 은신처는 `sc.clue_pts`에 들어가 집기가 그 자리를 피한다. 늘리면 그만큼 집기가 줄어든다.
- 수위(#141): 4층은 안전 구간(`floor_manager.JANITOR_FREE_FLOOR`), 3·2·1층에서 활동.
  - **은신은 '못 봤을 때'만 통한다**(#298). 예전에는 `_update_awareness()`가 `is_hiding`만 보고 추적을 **무조건** 끊어서, 눈앞에서 캐비닛에 들어가도 수위가 순찰로 돌아갔다. 즉시 해제 자체는 #6의 옳은 판단이다(유예를 주면 숨은 직후 접촉 판정 #4에 붙잡혀 은신이 무의미해진다) — 빠져 있던 것은 **봤는지 안 봤는지의 구분**이다.
    **숨는 그 순간**에 `chase_hold > 0` 또는 `seen_now`면 그 자리를 기억하고 수색에 들어간다(`search_point`·`search_timer`). 숨은 뒤에도 `player.position`은 은신처에 남아 `_can_be_seen()`이 계속 참일 수 있으므로 **전이 프레임에서만** 판정한다(`_player_hidden`).
    수색은 `search_speed`(175, 순찰과 추격 사이)로 걸어가 `SEARCH_OPEN_DISTANCE`(46) 안에 들면 은신처를 연다 — 안에 있으면 **먼저 끌어낸 뒤**(`set_hiding(false)`) 붙잡는다(숨은 채로 게임 오버가 뜨면 앞뒤가 안 맞는다). 비어 있으면 순찰로 돌아간다. **길이 막히면 포기해야 한다**(`STUCK_SECONDS`) — 안 그러면 그 자리에 멈춰 순찰이 죽는다.
    직접 보이면 추격이 수색보다 **우선**이고 그때 `search_timer`를 끈다. 음악·발소리는 수색도 추격과 같이 취급한다(`hunting`) — 위치를 알고 걸어오는 것이라 긴장이 같아야 한다. 수색 상태는 층을 넘기지 않는다. 순찰은 층 씬의 `Door_*`에서 뽑은 **문 앞 대기 지점을 최근접 이웃으로 이은 고정 루트를 왕복**하고, 문마다 1.8초 멈춰 방을 확인한다.
  - **문 확인이 끝나면 `SWEEP_CHANCE`(32%)로 방에 들어간다**(#321). 들어가 `SWEEP_LOOK_SECONDS`만큼 둘러보고 문 앞으로 되돌아 나온다. #313에서 순찰을 복도로 한정한 뒤 **방 안이 완전 안전지대**가 됐기 때문이다 — 수위가 방에 들어오는 것이 추격·수색(이미 들킨 뒤)뿐이라, 들키지만 않으면 방에서 무한정 뒤질 수 있었다. 실측 90초에 문 6곳·진입 2회(33%)로 대략 45초에 한 번이다.
  - 순찰은 `patrol_grid`(복도 전용)를 쓰므로 **진입·복귀만 `astar_grid`(전체)로** 길을 찾는다. 문 너머가 복도인 관통 문은 `corridor_lookup`으로 걸러 건너뛴다.
  - 시간(`SWEEP_LIMIT`)이 다 되거나 막히면 **나오는 단계로 넘긴다.** 나오다가도 막히면 그냥 끝낸다 — `_corridor_path()`가 방 안에 서 있는 경우 전체 격자로 복도까지 먼저 데려다 주므로(#313) 갇히지 않는다.
  - 추격이 시작되면 진입 상태를 버린다. 층을 옮겨도 초기화한다(같은 노드를 층마다 재사용한다). 들리는 거리(720px) 안에서만 혼잣말·열쇠 소리, 420px 안이면 발소리를 하단 알림으로 낸다.
  - **대사는 상황마다 풀을 두고 `_pick()`으로 고른다**(#330). 예전에는 상황마다 문장이 하나씩이라 한 판에 같은 말을 수십 번 들었다(소리 단서는 9초 쿨다운마다 뜬다). 풀은 혼잣말 6 / 발소리 5 / 열쇠 4 / 문 확인 4 / 발각 4 / 방 진입 4 / 수색 시작·실패·포기 3씩 / 붙잡힘 3 / 스턴·해제 3씩이다.
  - `_pick()`은 **바로 앞에 쓴 문장만 피한다.** `pick_random()`만 쓰면 3~5개짜리 풀에서 같은 말이 두 번 이어 나오는 일이 잦다. 완전한 비반복이 아니라 연속만 막는 것이라 상태가 풀당 문자열 하나뿐이다.
  - **소리 단서는 지문 톤**(`_say` → 화자 없음, 무엇이 들리는지)이고 **수위 대사는 말투**(`_say_line` → 화자 '수위')다. 박종태는 딸을 잃고 스스로 심판을 집행하는 인물이라 히스테릭하지 않고 담담해야 한다. 발각(접촉 30px) → 체포 게임 오버. 루트 점검은 `tools/verify_janitor_route.py`.

### 진행 요소 위치

- 본편은 4층 복도(579,692)에서 시작해 계단 열쇠로 3→2→1층 하강, 1층 수위실 금고에서 front_gate_key를 얻어 현관 탈출. **열쇠는 전부 접촉 획득**(E 불필요), 잉크통 등 조사 오브젝트만 E.
- 계단 열쇠(#159 재배치, **열쇠 하나당 획득처 하나**): stair_key_4 = 4층 다산7실 / stair_key_3 = 3층 2학년부 고리 / stair_key_2 = 2층 하단 남자화장실 배수구 / stair_key_1 = 1층 창고(하강엔 불필요). 중복 열쇠는 인벤토리(5칸)만 먹어서 걷어냈다 — 4층 컴퓨터실(#219)·4층 창고 태호 쪽지(#222)·3층 2학년부 보관함(#207)은 열쇠 없이 단서로만 남는다.
- 단서 오브젝트(#159/#161 선택지3로 새 맵에 맞게 개정 — **플래그 ID는 그대로**): 4층 다산7실(하람 메모)·창고(태호 쪽지)·창의체험부(시우 상담기록·그림)·정보부실(대응 매뉴얼·잉크통)·컴퓨터실(조민혁)·다산6실(졸업앨범), 3층 생활지도부(임나연)·2학년부(종태 기록·열쇠 보관함)·진로실(전단), 2층 체육창고(강유진)·하단 남자화장실(끌린 자국)·교육실(백승호), 1층 교무실(교장 편지)·수위실(사진벽·학생증·공책·금고).
- 단서 본문(메시지·플래그·스크립트)은 `tools/story_objects.json`에 있고 `gen_floors.py`의 `PLACEMENT`가 방만 지정한다. 방을 옮기려면 PLACEMENT만 고치면 된다.
- 엔딩은 "방과 후" 1종(러닝타임 축소, 2026-07-28 사용자 결정) — 현관에서 분기 판정이 없다. 단서 플래그는 조사 기록용이며 진행을 막지 않는다. 신고 선택지·숨은 엔딩·재조사는 넣지 않는다.
- 새 기획서 기준 스토리 작업(#137 1~4단계)은 전부 끝났다. 옥상 씬은 계획 없음.

### 개발 시 주의

- 이 환경에는 Godot 바이너리가 없음 — 실행 검증(F5)은 사용자가 수동으로 함. 정적 검증(기하·경로 대조 스크립트)을 기록하고 PR을 연 뒤 사용자 확인을 기다린다.
- `ci_load_scenes.gd`는 씬을 instantiate만 하고 트리에 붙이지 않는다 — **`_ready`가 돌지 않으므로 노드 경로·시그널 연결은 검증되지 않는다.** 미닫이문처럼 `_ready`에서 자식을 찾는 스크립트를 넣었으면, 층 씬을 `root.add_child()`로 붙이는 임시 스크립트로 따로 확인할 것.
- .tscn 수정 시 `load_steps` = ext_resource 수 + sub_resource 수 + 1 유지.
- project.godot에 사용자의 미커밋 변경이 있을 수 있음 — 내 커밋에 섞지 말 것(필요 시 stash로 분리).
- .gd 스크립트를 새로 만들면 사용자 에디터가 .uid 파일을 생성함 — 발견 시 해당 이슈 브랜치에 커밋.

## Issue-First Rule

- 기능, 버그 수정, 개선, 리팩터링 작업은 GitHub 이슈 없이 구현을 시작하지 않습니다.
- 사용자가 이슈 없이 작업을 요청하면 GitHub 접근 권한이 있을 때 먼저 이슈를 만듭니다.
- GitHub 접근 권한이 없으면 사용자에게 이슈 없이 진행해도 되는지 확인하고, 최종 응답에 이슈 생성이 막혔다는 점을 남깁니다.
- 이슈 번호는 브랜치 이름, 커밋 메시지, PR 본문에 포함합니다.
- 관련 없는 정리 작업은 별도 이슈와 별도 브랜치로 분리합니다.

## Required Issue Detail

모든 기능, 개선, 버그 이슈에는 아래 항목이 있어야 합니다.

- Summary: 무엇이 바뀌어야 하는지.
- Motivation or Problem: 왜 필요한지.
- Current Behavior: 현재 어떻게 동작하는지.
- Expected Behavior: 완료 후 어떻게 동작해야 하는지.
- Scope: 영향을 받을 게임 시스템, 씬, 스크립트, 에셋, 문서.
- Acceptance Criteria: 완료를 증명할 구체적인 기준.
- Verification Plan: 실행할 명령이나 수동 확인 방법.

버그 수정 이슈에는 추가로 아래 항목이 필요합니다.

- Reproduction Steps.
- Actual Result.
- Expected Result.
- Environment, when relevant.

새 기능 이슈에는 추가로 아래 항목이 필요합니다.

- Player Flow.
- Non-goals.
- UX, input, balance, or settings expectations, when relevant.

## Branching

- 이슈 하나당 브랜치 하나를 만듭니다.
- 브랜치 이름은 짧고 이슈 번호를 포함합니다.
- 권장 형식:
  - `issue-<number>-short-topic`
  - `fix-<number>-short-topic`
  - `feat-<number>-short-topic`

## Implementation

- 파일을 수정하기 전에 이슈를 읽고 의도한 동작을 확인합니다.
- 변경 범위는 이슈에 적힌 내용으로 제한합니다.
- 기존 프로젝트 패턴을 우선합니다.
- 큰 구조 변경이나 폴더 정리는 해당 이슈가 직접 요구할 때만 합니다.

## Verification

변경한 파일과 게임 엔진 상태에 맞춰 가장 작은 의미 있는 검증부터 실행합니다.

- 맵 관련 변경은 **`python3 tools/gen_floors.py` 재생성 후** 아래 4개를 모두 돌립니다:
  `verify_scenes.py`(정합성·깨진 실수 값) / `verify_floor_reach.py`(방 도달성·막힌 공간 봉인·카메라 한계) /
  `verify_stairs.py`(floor_manager 계단 좌표 ↔ 씬 대조) / `verify_progression.py`(4층→1층 현관 완주 가능) /
  `verify_props.py`(방 집기는 방 안, 복도 집기 `Corr_*`는 방 밖, 소품 `PT_`는 집기 안. 벽·다른 집기·단서와 겹치지 않는지, 미닫이문이 **열린 자리**에서 집기와 겹치지 않는지. 사선 벽이 있어 분리축으로 본다).
  은신처를 건드렸으면 `verify_hiding_spots.py`도 함께 돌립니다.
- 씬이나 스크립트를 고쳤으면 **푸시 전에 `python3 tools/verify_scenes.py`를 실행**합니다.
  load_steps, 리소스 참조, 노드 부모 경로, 형제 이름 중복, 스크립트 $NodePath,
  벽 충돌↔광원 차단체 1:1을 검사합니다(Godot 불필요, 수초).
- 타일 무늬를 고쳤으면 `python3 tools/gen_tiles.py` 후 `python3 tools/gen_floors.py`까지
  다시 돌립니다(생성기가 평균 휘도·명암·주기성·도트 격자·선 없음을 스스로 검사하고,
  결정론적이라 무늬를 안 바꿨으면 diff가 나오지 않습니다).
- 열쇠 스프라이트나 원본 아트를 고쳤으면 `python3 tools/gen_key_sprite.py` 후 `python3 tools/gen_floors.py`
  까지 다시 돌립니다(생성기가 배경 제거·고리 구멍·빈 스프라이트를 스스로 검사하고,
  결정론적이라 원본을 안 바꿨으면 diff가 나오지 않습니다).
- 효과음을 고쳤으면 `python3 tools/gen_sfx.py`, 앰비언트·BGM은 `python3 tools/gen_music.py`로
  재생성합니다(길이·피크·DC 오프셋·루프 이음매를 스스로 검사하고, 결정론적이라
  톤을 안 바꿨으면 diff가 나오지 않습니다).
- 수위 순찰·문 배치를 건드렸으면 `python3 tools/verify_janitor_route.py`
  (문 앞 대기 지점이 벽에 안 박히는지·계단에서 닿는지·루트가 맵을 가로지르지 않는지).
- 기하·좌표를 바꿨으면 해당 이슈에 맞는 임시 검증 스크립트로 대조합니다.
- 플레이어 입력, UI, 충돌, 게임 흐름 확인은 사용자의 수동 F5에 의존합니다.
- 푸시하면 GitHub Actions(`.github/workflows/ci.yml`)가 정적 검사와 함께
  Godot 4.6 헤드리스 임포트·전체 씬 로드를 실행합니다. 결과는 `gh pr checks <번호>`로 확인합니다.
  씬 로드는 `tools/ci_load_scenes.gd`가 맡고 씬뿐 아니라 `scripts/` 아래 `.gd`도 전부
  컴파일해 봅니다. 이 스크립트는 **`_init()`이 아니라 `_initialize()`에서 돌아야 합니다**(#183) —
  `--script`로 넘긴 MainLoop는 autoload(`Sfx`) 등록보다 먼저 만들어져서, `_init()`에서
  로드하면 `Sfx` 참조 스크립트가 전부 컴파일에 실패합니다(그래도 씬 로드는 성공해 검사가 통과함).

PR 본문에는 실제로 확인한 내용을 기록합니다.

## Pull Requests

- PR 제목은 이슈에서 해결한 결과를 요약합니다.
- PR 본문에는 `Closes #<issue-number>`를 포함합니다.
- PR 본문에는 summary, verification, residual risks를 포함합니다.
- 검증 내용이 기록되기 전에는 머지하지 않습니다.

## Merge Flow

- 이슈를 해결하고 검증을 마친 뒤 이슈 브랜치에 커밋하고 원격 저장소에 푸시합니다.
- 이슈 브랜치에서 `dev`로 첫 번째 PR을 엽니다.
- PR이 mergeable/CLEAN이고 CI 체크가 통과했고 변경 파일이 이슈 범위와 일치하면 자동으로 머지합니다.
- CI가 실행 중이면(mergeStateStatus가 UNSTABLE 등) 완료를 기다린 뒤 판단합니다. 실패하면 머지하지 않고 원인을 고칩니다.
- 같은 원격 이슈 브랜치에서 `main`으로 두 번째 PR을 열고, 같은 기준을 확인한 뒤 자동으로 머지합니다.
- `main`은 브랜치 보호(리뷰 승인 1개 필수)가 있으므로, 기준을 충족한 PR은 admin 권한(`gh pr merge --admin`)으로 머지합니다. (사용자 승인: 2026-07-05)
- `main` 머지가 끝나면 원격 이슈 브랜치를 삭제합니다.
- 정리 후 로컬 저장소는 삭제된 이슈 브랜치가 아니라 `main` 또는 `dev`에 둡니다.

자동 머지를 멈추고 사용자에게 보고하는 예외:

- 코드 충돌이 있거나 mergeable/CLEAN이 아닌 경우
- 이슈 범위 밖의 파일 변경이 섞인 경우
- 검증이 누락되었거나 미완인 경우(예: 자격증명·바이너리 부족으로 실행 확인 불가)
- 사용자가 "머지하지 말라"고 지시한 경우
- 되돌리기 어려운 부수효과가 있는 경우(데이터 마이그레이션, 배포 트리거 등)
